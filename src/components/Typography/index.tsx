import tw, { styled } from "twin.macro";

export const HeadingL = styled.h1(() => [
  // The common button styles added with the tw import
  tw`text-4xl font-bold text-08`,
  // Some additional component specific styles
]);
